# 批量开户与上下文优化验收

日期：2026-09-22。分支：`fix/crm-batch-root`。起点：`5b3ebf7a12be5892d941fbc21966e37ba3e81007`。

本轮直接更新原型仓库及原 PRD 入口。视频录制继续暂停。未操作生产平台，未重置用户 4317 原型数据。

## 已交付行为

- 本人被指派的开户媒介在原申请表内录入供应商、账户 ID、选填名称和纵横 ID；系统管理员沿用既有代办权限，其他业务角色查看结果。
- 供应商可搜索，可给选中行批量填入。默认仅填空值；替换已填供应商需要明确选择覆盖方式。
- 保存全部草稿与登记选中账户成功分开。登记前校验全部选中行，任一错误都不写入该批账户；重复双方按原申请行号标红。
- 已成功、已取消、结果待核实、待同步行不参与批量修改。异常通过单行入口处理；失败或未知输入继续保存为草稿，核实未发生后可续办。
- 所有行完成后仍需点击“完成本次开户”；纵横 ID 可在完成后继续补充。
- 切换角色/人员前保护未保存输入；商务 B 刷新后保持 B；申请列表搜索和状态筛选按身份保留。
- 从共享主体发起开户时，只有唯一关联客户才自动带入；多客户必须明确选择本次客户。

## 实际浏览器验收

使用 4318 独立验收源，原型代码与 4317 一致，浏览器数据隔离。验收申请：`KH-mucikrms-25h0`，客户“开户续办验收0922”，主体“开户续办验收有限公司”，店铺“开户续办体验店”。

| 场景 | 观察结果 |
| --- | --- |
| 商务复用客户、主体、店铺，数量增加至 10 后提交 | 同一申请保留 10 行，商务页面无批量登记入口 |
| 主管指派，媒介二查看，媒介一办理 | 他人只读；本人出现表格输入及批量操作，无主管指派按钮 |
| 搜索供应商，批量填入，改第 10 行供应商 | 默认只填空值不会覆盖已选项；明确覆盖后可统一替换 |
| 第 1、3 行填重复 ID 后登记 | 成功数量保持 0；双方提示；材料缺失也阻止整批登记 |
| 保存草稿，改正第 3 行后切换身份 | 弹出保护；保存并切换后，他人只读；切回及刷新仍保留新值 |
| ID 输入框按 Tab | 焦点连续进入同一行账户名称输入框 |
| 最末行供应商下拉 | 修复表格裁切与过宽，列表显示全部候选；可再次打开 |
| 选择前 8 行登记，退出至工作台 | 仍为一条原申请，显示申请 10、成功 8、待处理 2，提供“继续办理” |
| 从待办回原单，筛选待填写 | 第 9、10 行草稿保持，已成功行锁定 |
| 第 9、10 行重复，切换“有问题”后更正 | 提示使用第 9、10 行；编辑时不突然隐藏该行；更正后两行登记成功 |
| 全部 10 行成功 | 状态仍是“待确认完成”，完成按钮可用，未自动结束 |
| 点击完成并确认 | 申请显示开户已完成，10 个选填纵横 ID 待补不阻止结束 |
| 列表输入申请号、筛选已完成，进详情再返回、刷新 | 查询词与状态保持 |
| 商务切换 B 后刷新 | 仍显示商务 B |

证据截图：[八户完成仍可续办](批量开户-八户完成仍可续办.png)、[十户完成](批量开户-十户完成.png)。

补测失败草稿场景时，内置浏览器连接中断，恢复标签/新建页面均未成功附加；该项没有追加浏览器通过声明。修复已由专项模型测试及独立源码复核确认，详见下一节。共享主体多客户拦截由渲染测试验证，本次没有为此修改历史样本关系。

## 自动化及独立复核

- `npm.cmd test`：160/160 通过。批量专项 16 项，含整批回滚、长 ID/前导零、10 选 8、转派权限、锁定行、附件和流程前置、失败草稿刷新续办、未知核实后保留输入。
- `npm.cmd run test:render`：24/24 通过，另含批量角色入口、成功行锁定、共享主体客户选择、筛选初值检查。
- `npm.cmd run build`：通过；保留现有较大 bundle 警告。
- `npm.cmd run export:standalone`：通过，外部资源数量为 0；更新原单文件交付入口。
- 独立审查固定提交 `9764ab947c3f6e9a81dcce802e71a41ea868840c` 找到单行失败后删除草稿问题。修复提交 `623a9ddb07f02475d404caa2b4415171d81050ab` 已独立复核，通过 16 项批量专项测试，无新增实质阻塞。
- 原访问地址 `http://127.0.0.1:4317/` HTTP 200。

## 原 PRD 同步

原目录：`D:\PROJECT\薪火CRM系统\重构调研\PRD评审稿-20260921`。文件名继续使用 `薪火CRM-完整PRD-V1.0`，内部版本为 V1.4。

Markdown、HTML、DOCX、PDF 四格式读回通过，40 个模块、24 张图齐全，PDF 78 页。M01/M06 规则及验收更新，其他 38 个模块正文未改；同步图 4-9、5-1、5-9，保留原财务口径。54 项清单与交付 ZIP 按落地文件重新计算哈希，包内不含 QA、缓存或浏览器资料。四种正文文件与原目录 SHA256 一致。

更新前已备份至 `D:\PROJECT\薪火CRM系统\重构调研\历史版本\PRD批量开户修复前-20260922-061818`。

## 后续优化候选（未实施）

1. Excel 多行粘贴：适用于供应商已给出多账户清单，复用现有整批校验，减少逐格输入。本轮不包含导入。
2. 按人员管理未提交草稿：当前商务申请仍使用单草稿槽位；需要明确每人一份还是多份，再确定草稿箱、隔离与覆盖规则。
3. 通知按申请汇总并区分接收人：避免多个纵横待补通知淹没申请任务，个人已读和主管查看的关系尚待确认。

这些建议未改变本轮已确认业务规则或扩大交付范围。
