# 原PRD与原原型更新验收

验收日期：2026-09-22。改动按用户本轮确认：主体与直客一对一、直客名称等于主体名称，单主体单店，商务一页资料与数量，媒介逐行办理。

## 原文件更新

- 原原型目录：`D:\PROJECT\薪火CRM系统\交互原型-审查修订版-20260921`，原入口 `http://127.0.0.1:4317/` 已实际打开核验新页面。
- 原单文件原型：`交付/薪火CRM-完整可交互原型.html` 已重新构建导出，不依赖外部资源。
- 原PRD目录：`D:\PROJECT\薪火CRM系统\重构调研\PRD评审稿-20260921`，沿用原 V1.0 文件名，正文当前版本 V1.2，四格式及关系图原位更新。
- 更新前完整备份：`D:\PROJECT\薪火CRM系统\重构调研\历史版本\PRD原文件-更新前-20260922-0336\PRD评审稿-20260921`。
- 原型页内规则已从当前40模块同源契约同步。真实历史样本未改写。

## 自动检查

- `npm.cmd test`：122/122 通过，覆盖新关系、数量、空名称、编号唯一、快照、资质校验及既有财务回归。
- `npm.cmd run test:render`：24/24 通过，覆盖核心、运营和财务渲染。
- `npm.cmd run build`、`npm.cmd run export:standalone`：通过。构建仍有单包超过500 KB提示，不影响产物生成。
- `git diff --check`：通过。
- 独立审查发现并修复：异步上传跨上下文回写、附件必需却无适用上传入口、切换主体后误复用原执照。

## 浏览器实际操作

使用本地4318验收实例隔离演示状态，最后打开原4317确认同源新页面。通过可见UI完成以下操作，没有直接注入浏览器业务状态。

1. 输入演示客户名称查重，原地新建客户与主体；直客名称自动复用主体公司名称，填写一个演示直客ID。主体资料保存后新增唯一店铺，来客ID留空。
2. 商务数量执行 `1 → 2 → 3 → 2`；没有逐行账户或逐行直客编辑区域。
3. 上传 `demo-qualification.png`（明显标注非真实执照）；实际打开图片预览，保存草稿，刷新后从“继续未提交草稿”恢复数量2及附件，预览仍可用。
4. 提交2户申请，媒介主管指派，开户媒介收到2行任务。缺供应商时被拦截；选供应商后，两户仅填写不同广告账户ID、账户名和纵横ID留空，均成功落档，共用主体直客。进度依次为0/2、1/2、2/2，最后全部完成。
5. 配置本地推营业执照为“媒介执行前补齐”，发布本地v2；新单未上传仍能由商务提交，媒介尝试成功结果时明确拦截；在原单补上传后成功完成。新旧单各用提交时规则。
6. 新申请上传完成后更改主体搜索，原附件解除本申请关联并出现提示；未选主体时不提供上传按钮。
7. 编辑中的结果弹窗关闭出现未保存提示；放弃仅退出该输入。最终原4317既有申请和配置仍保留。
8. 浏览器检查未发现控制台error；实际查看新增页、附件预览及媒介结果。页面窄幅下可用，补齐了主体检索区与数量控件的卡片内边距。

## 范围与限制

附件真实文件保存在当前浏览器IndexedDB，元数据随本地演示状态保存。未连接生产文件存储、媒体开户接口或企业审批系统；本次不代表生产上线验证。历史一主多店保留原样并提示核对，新申请遵守一主一店，不自动合并历史资料。
