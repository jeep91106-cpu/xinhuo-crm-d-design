# V1.5 效率改进验收

日期：2026-09-22。协调与整合：Codex。起点 `505572f4ebbec3ccb23511ee958fa2a4b48bddb0`，整合分支 `feat/crm-efficiency-root`。直接更新原原型与原 PRD，录屏仍暂停。

## 范围和独立审查

- 个人多份未提交草稿：创建人隔离、继续、保存、另存、可恢复删除、提交仅消费当前份；保留旧全局草稿兼容路径。
- 按申请聚合通知：角色范围、每人独立已读、业务变化重新提醒、统一顶部计数与原申请入口。
- Excel TSV 多行粘贴：文本 ID、部分表头、原行映射、预览后确认填表；不代替登记和完成整单。
- 独立审查固定 `e88ad62` 发现中间空行压缩、陈旧预览转填及负号公式遗漏，均已在 `d500b991e2fc4c736c4b6c430b47741b4f851f1b` 修复并独立复核通过。后续 PRD 增量精确对应空白不覆盖和不可编辑状态边界。
- 存储写入失败不再更新界面为已保存；错误向上传递，保存并离开/切换身份继续保留当前输入。

## 实际浏览器验收

使用 Chrome 中的独立测试源 `http://127.0.0.1:4318`，真实操作表单与按钮，未直接注入应用状态。原用户演示源 `4317` 的申请与配置未用于测试写入。

1. 商务 A 选择已有客户“开户续办验收0922”，关联主体和唯一店铺正常展示；数量调整为 10。
2. 保存 `DRAFT-mucpfabf-uozk`，再另存为 `DRAFT-a398ec26-bcb2-4e71-8c6b-d230e2cfe773`，草稿箱显示两份，各 10 户。
3. 切换商务 B，草稿箱显示 0；直接打开 A 的草稿地址，显示“草稿不存在或不属于当前人员”。切回 A 可恢复原草稿。
4. 提交第一份草稿，生成原申请 `KH-mucpmj7j-2962`；主管指派开户媒介一。另一份草稿仍保留，提交后 A 的草稿箱显示 1。
5. 在媒介批量表粘贴科学计数法及重复 ID，预览显示对应原行错误，确认填入禁用；未写入任何账户。
6. 更正为 10 行制表符文本（供应商“本地推-迪雾”），预览显示 10 行、30 项修改。`0009222600000000001` 至 `0009222600000000010` 的 19 位含前导零编号完整保留。
7. 确认只填表，显示未保存修改；点击“保存全部草稿”后退出工作台。工作台仅一笔待办，显示申请 10、成功 0、待处理 10；“继续办理”回到同一申请，10 行均显示草稿已保存，首末编号回显准确。
8. 点击“登记选中账户成功（10）”，10 行全部成功，原单停在“待确认完成”。再点击“完成本次开户”与“确认完成”，原单完成，成功 10、待处理 0，纵横待补 10。
9. 消息中心按该申请只显示一条，内容为申请 10、成功 10、待办 0、纵横待补 10，原单号保持 `KH-mucpmj7j-2962`。
10. 媒介将该条标为已读，未读数由 8 变为 7；切换商务 A，同一通知仍未读、总未读 8。商务点“打开开户申请”，回到完整的同一原单与 10 个账户。

界面读回证据：`V1.5-草稿箱-UI.txt`、`V1.5-消息中心-UI.txt`。本轮浏览器截图接口返回零宽度/输入超时，未生成新的截图，不将文本界面读回记作截图检查。临时视口覆盖已恢复。

## 附件与环境边界

Chrome 文件选择器设置验收文件时返回 Not allowed，未把附件上传记为成功。为验收本轮三项效率功能的完整闭环，仅在 4318 临时发布“附件可选”的 v3 后提交测试申请；流程完成后已发布 v4，将营业执照恢复“媒介执行前补齐”。原 4317 配置未更改。在途测试申请正确保留其提交时 v3 快照。既有附件校验、跨主体保护等由本次完整回归测试覆盖。

可恢复删除、旧草稿迁移、转派、陈旧粘贴目标、全批原子写入等额外边界通过定向自动化与独立代码审查覆盖；未声称均在浏览器点击验证。站内通知为本地原型数据，不发送企业微信或其他外部消息；未操作生产系统。

## 命令与结果

- `npm.cmd test`：198/198 通过，包含新增 16 条草稿、12 条通知、10 条粘贴测试。
- `npm.cmd run test:render`：24/24 通过。
- `npm.cmd run build`：通过。
- `npm.cmd run export:standalone`：通过，1,048,645 字节、215 个样本账户、0 个外部资源。
- PRD 暂存入口与原入口分别执行 `scripts/check_prd.py` 和 `scripts/package_prd.py`：通过；四格式规则读回、80 页 PDF、40 模块、24 图、54 项清单及 55 项 ZIP 条目完整。
- PRD 原入口：`D:\PROJECT\薪火CRM系统\重构调研\PRD评审稿-20260921`；原文件名沿用 V1.0，正文内部 V1.5。覆盖前备份：`D:\PROJECT\薪火CRM系统\重构调研\历史版本\PRD效率改进前-20260922-093825`。

独立工作提交：草稿 `d989087e`；通知 `43c2e3cf`；PRD `79b5c926`、`88097d64`。主分支未推送、未合并、未发布外部站点。
