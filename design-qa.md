# 薪火 CRM 原型 v0.2 设计 QA

## 比较对象

- Source visual truth：`02-原型/qa/reference-field-template.png`（用户提供的 v0.1 字段模板截图，2543 × 1332 px）。
- Implementation：`02-原型/qa/implementation-desktop.png`（v0.2 字段与表单规则，1920 × 1080 px）。
- Mobile implementation：`02-原型/qa/implementation-mobile.png`（390 × 844 px）。
- Combined comparison：`02-原型/qa/comparison-field-template.png`。
- Inline-feedback source：`02-原型/qa/reference-feedback-empty.png`（用户指出空清单入口不够直接）。
- Inline-feedback implementation：`02-原型/qa/implementation-feedback-inline.png`（2550 × 1365 px）。
- Inline-feedback comparison：`02-原型/qa/comparison-feedback-inline.png`。
- Page-edit implementation：`02-原型/qa/implementation-page-edit-desktop.png`（2550 × 1365 px；CSS 1700 × 910，deviceScaleFactor 1.5）。
- Page-edit mobile：`02-原型/qa/implementation-page-edit-mobile.png`（390 × 844 px，deviceScaleFactor 1）。
- Page-edit comparison：`02-原型/qa/comparison-page-edit.png`。
- Desktop CSS viewport：1280 × 720，deviceScaleFactor 1.5；源图是不同密度与尺寸的评审截图，因此按同一桌面状态做结构、风格和交互意图比较，不做逐像素复制。
- Mobile CSS viewport：390 × 844，deviceScaleFactor 1。
- State：系统管理员 / 巨量本地推 / 商务申请 / AGENT 自有端口 / 无字段反馈数据。

## 全视图比较

- 延续 v0.1 的暖白底、橙色主动作、左侧导航和圆角卡片视觉语言，没有另起风格。
- v0.1 的大面积空白已被“生产事实、模板维度、规则健康度、字段规则表”替代，信息密度提高但仍保持分组留白。
- v0.1 顶部六个抽象角色胶囊改为真实人工角色选择器；角色上下文显示人数、数据范围与组织，减少拥挤并解决角色口径错误。
- v0.1 的四列表格扩展为对象、角色、显隐、必填、来源、条件/校验和操作；新增字段草稿、字段反馈、预览和发布治理入口均在首屏可发现。

## 重点区域比较

- 模板作用域：媒体产品、业务阶段、供应商渠道三个选择器并列，解决旧版“本地推/非本地推”与产品、供应商互相冲突的问题。
- 字段规则：纵横 ID 不再出现在商务申请；来客 ID、巨量直客 ID、资质与返点的来源和归属已按本轮数据库事实修正。
- `＋提交字段调整`：全局按钮、悬浮入口和字段行级“反馈”都打开同一需求单；提交只写浏览器 localStorage，可导出 JSON。
- 窄屏：导航改为横向滚动快捷条，配置表单单列，宽表保留水平滚动；没有页面级横向溢出或控件重叠。

## 必查面

- Fonts and typography：使用系统中文字体栈，标题、正文、证据标签和表格层级清楚；长说明可自然换行。
- Spacing and layout rhythm：桌面端使用 248px 侧栏、16px 内容间距和一致卡片内边距；移动端收敛为单列。
- Colors and visual tokens：暖白、橙色动作色与原稿一致；生产事实用低饱和绿色，警告和错误同时带文字，不只靠颜色。
- Image quality and assets：界面没有业务图片资产；品牌方块沿用原稿的抽象标记，不新增占位插画、SVG 或低清图片。
- Copy and content：真实生产聚合与“演示待办”明确区分；所有新字段均标注为目标态或新主数据字段，不暗示线上已具备。
- Accessibility：语义按钮、表单 label、焦点样式、`aria-live`、弹窗 Esc 关闭与 Tab 循环已实现；移动端可达。

## 交互验证

- 实际角色切换：商务只有本人/协作入口；系统管理员能访问配置；RPA 不出现在人工选择器。
- 字段建议：填写字段、期望和原因后，本机清单数量从 0 变 1，清单可读。
- 页面内填写：点击清单内 `＋` 后表单直接在页面展开；隔离测试提交后数量从 1 变 2，新卡片立即显示在同一页面。
- 全局自由编辑：标题、说明、按钮文案、表单值可改；字段、表格行、卡片可增加和移除；保存刷新持久化、取消撤销、恢复默认均通过。
- 隔离：商务工作台与客户档案不串改；字段模板的本地推与巨量 AD 视图不串改。
- 安全：输入 `<img src=x onerror=alert(1)>` 保存后仍为纯文字，未生成图片或执行代码；删除充值计算字段后按钮给出安全提示，无控制台错误。
- 新增字段：新增 `open_category_code` 草稿后当前字段数从 8 变 9，基础冲突校验通过。
- 开户申请：填写 19 位来客 ID、增加账户需求、提交校验通过；新增需求时其他表单值不丢失。
- 开户处理：批量粘贴三行后，两个 16 位账户通过，一个错误 ID 被逐行标红。
- 浏览器控制台：0 条 error / warning。

## 比较历史与修复

### Pass 1

- P1：增加账户需求时整页重渲染，已填来客 ID 丢失。
- P2：390px 窄屏把完整侧栏纵向铺在正文前，进入正文距离过长。

### 修复

- 账户需求改为只更新明细表区域，不重渲染主表单。
- 窄屏导航改为 sticky 横向快捷条；无权限项隐藏，正文直接进入视口。

### Pass 2

- 同一开户路径复测通过，来客 ID 保留且提交成功。
- 390 × 844 截图显示导航、标题、角色上下文、生产事实和首个配置卡均可见。
- 未发现仍需修复的 P0 / P1 / P2。

### Pass 3：页面内填写

- P1：原实现点击全局 `＋` 使用弹窗，用户无法在清单上下文中边看边填；空页面仍显得像没有可操作区域。
- 修复：清单增加大号 `＋` 占位入口；点击后直接替换为页面内表单。全局、页头和字段行入口统一导航到该表单。
- 提交后表单自动收起，新增建议卡片立即留在当前页面；本机持久化与 JSON 导出保持不变。
- 并排视觉证据见 `02-原型/qa/comparison-feedback-inline.png`；未发现新的 P0 / P1 / P2。

### Pass 4：全局自由编辑

- P1：原型内容只能由开发改代码，业务人员不能在页面上直接调整文字、字段和结构。
- 修复：增加全局“编辑页面”；进入后用虚线标出可编辑文字，字段/表格行/卡片/任务/规则提供 `－`，表单/表格/列表/网格提供对应 `＋`。
- 持久化：使用独立 `xinhuo.pageEdits.v1` 视觉补丁层，只保存纯文字、控件值、删除标识和结构化新增项；按页面及媒体/阶段/渠道变体隔离。
- 防回归：编辑时锁定导航、角色和业务动作；保存后原交互恢复。已验证取消、恢复默认、刷新持久化、跨页面/跨产品隔离、字段值保存、`Ctrl+S` 和删除功能字段后的安全提示。
- 视觉：1700 × 910 桌面和 390 × 844 窄屏均可见编辑工具栏与增删控件，无覆盖核心内容的 P0/P1/P2 问题。
- 并排证据见 `02-原型/qa/comparison-page-edit.png`；未发现仍需修复的 P0 / P1 / P2。

## Follow-up polish

- P3：移动端横向导航只露出前几个入口，需要用户滑动；后续可加“更多”菜单，但不阻塞当前业务评审。
- P3：真实上线可引入正式品牌 Logo 与图标库；本轮维持原型已有的抽象品牌标记。

final result: passed
